HA:
    nodes:
        eosnode1: ${eosnode1_name}
        eosnode2: ${eosnode2_name}
    management:
        vip:
            ip: ${mgnt_ip}
            nic: ${mgnt_nic}
            netmask: ${mgnt_netmask}
